"use strict";

$('#onclickbtn').click(function() {
    var self = this;
    change(self);
 });

  function change( el ) {
   if ( el.value === "Show Options" )
   el.value = "Hide Options";
   else
   el.value = "Show Options";
  }

  $('#onclickbtn2').click(function() {
    var self = this;
    change(self);
 });

  function change( el ) {
   if ( el.value === "Show Options" )
   el.value = "Hide Options";
   else
   el.value = "Show Options";
  }

  $('#onclickbtn3').click(function() {
    var self = this;
    change(self);
 });

  function change( el ) {
   if ( el.value === "Show Options" )
   el.value = "Hide Options";
   else
   el.value = "Show Options";
} 

$('#onclickbtn4').click(function () {
   var self = this;
   change(self);
});

function change(el) {
   if (el.value === "Show Options")
      el.value = "Hide Options";
   else
      el.value = "Show Options";
} 

$('#onclickbtn5').click(function () {
   var self = this;
   change(self);
});

function change(el) {
   if (el.value === "Show Options")
      el.value = "Hide Options";
   else
      el.value = "Show Options";
}

$('#onclickbtn6').click(function () {
   var self = this;
   change(self);
});

function change(el) {
   if (el.value === "Show Options")
      el.value = "Hide Options";
   else
      el.value = "Show Options";
}